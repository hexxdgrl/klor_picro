# This is where most of the rules are stored
DEFAULT_FOLDER = klor_fix/klorkb

# Drivers
SERIAL_DRIVER = vendor
WS2812_DRIVER = vendor
OLED_DRIVER = ssd1306

# Enable encoder, this is included with the keyboard by default and should remain unchanged even in keymap rules.
ENCODER_ENABLE = yes
